// Create variable named numbers

var numbers=[];

// Create a for loop that adds numbers 1-9 to the var "numbers"
for(i=0; i<=9; i++){
    numbers.push(i);
}

// Console log first item in array
console.log(numbers[0]);

//Console log the last number in the array.
console.log(numbers[9]);

// Create a variable named car and assign an empty object to it.
var car={}

// Give the object a property called colour and assign it the value of "blank"

car.colour ="blank"